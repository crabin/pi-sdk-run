PORT=3000
HOST=127.0.0.1
PI_CODING_AGENT_DIR=.pi-config
PI_SESSION_DIR=.pi/sessions
PI_SESSION_MODE=continue-recent
# fail: 损坏/不可读时停止；new: 记录警告后创建新会话
PI_SESSION_RECOVERY=fail
PI_RESOURCE_MODE=project-only
# PI_PROVIDER=openai
# PI_MODEL=gpt-5

# dg-piagent migration record

The initial project copy was made recursively from the repository-level
`skills/dg-piagent` directory. Before any project-local repair, `diff -rq`
reported no differences and no symbolic links were present.

`source-manifest.sha256` records the SHA-256 digest of every initial source
file. `changes.json` records deliberate project-local changes made after that
copy. The repository-level source remains untouched.
